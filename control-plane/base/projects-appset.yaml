---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: product-areas-apps
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/jhollowayjc/argocd-gitops-playground
      revision: refactor
      files:
      - path: product-areas/**/project.yaml
  template:
    metadata:
      name: 'product-area-{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/jhollowayjc/argocd-gitops-playground
        targetRevision: refactor
        path: product-areas
        helm:
          valueFiles:
            - '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
